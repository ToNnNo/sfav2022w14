{% extends 'base.html.twig' %}

{% block title %}Editer un article{% endblock %}

{% block body %}

    <div class="mb-3">
        <a href="{{ path('article_index') }}" class="btn btn-outline-secondary btn-sm">Retour</a>
    </div>

    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button class="btn btn-outline-primary">Enregistrer</button>
    {{ form_end(form) }}

{% endblock %}
